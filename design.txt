main ->

Take input (test or file name) (and remember)
Call getHex() to get hex string
Pad 0' s and remember how many 0's were padded
Call encryptFull() function and get encrypted hex
Call DycryptFull() function and get decrypted hex
Remove padded 0's
Call GetOutput() to convert hex into text or file
Show the output

encryptFull ->
Take 16 bytes of data each with a loop and call encryptBlock() function, and concatenate the outputs
